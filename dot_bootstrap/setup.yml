---
- name: Machine setup
  hosts: localhost
  become: true
  connection: local
  gather_facts: true
  vars:
    nvim_version: "v0.11.4"

  tasks:
    - name: Get my user
      ansible.builtin.set_fact:
        remote_regular_user: "{{ ansible_env.SUDO_USER or ansible_user_id }}"

    - name: Install packages by apt
      ansible.builtin.apt:
        name:
          - 7zip
          - ca-certificates
          - curl
          - btop
          - eza
          - fd-find
          - ffmpeg
          - fish
          - fzf
          - git
          - imagemagick
          - jq
          - poppler-utils
          - preload
          - remmina
          - ripgrep
          - zoxide
        state: present

    - name: Install packages by flatpak 
      community.general.flatpak:
        name: 
          - com.vscodium.codium
          - com.obsproject.Studio
          - org.videolan.VLC
          - org.gimp.GIMP
        state: present

    - name: Change shell to fish
      ansible.builtin.user:
        name: "{{ remote_regular_user }}"
        shell: /usr/bin/fish

#    - name: Add Brave Browser Repo
#      ansible.builtin.apt_repository:
#        name: brave-browser
#        description: Brave Browser
#        baseurl: https://brave-browser-rpm-release.s3.brave.com/x86_64/
#        gpgkey: https://brave-browser-rpm-release.s3.brave.com/brave-core.asc
#        gpgcheck: true
#        enabled: true
#
#    - name: Import Brave Browser GPG Key
#      ansible.builtin.rpm_key:
#        key: https://brave-browser-rpm-release.s3.brave.com/brave-core.asc
#        state: present
#
#    - name: Install Brave Browser
#      ansible.builtin.apt:
#        name: brave-browser
#        state: present
#
#    - name: Add VSCodium Repo
#      ansible.builtin.apt_repository:
#        name: codium
#        description: VSCodium
#        baseurl: https://download.vscodium.com/debs
#        gpgkey: https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg
#        gpgcheck: true
#        enabled: true
#
#    - name: Import VSCodium gpg key 
#      ansible.builtin.rpm_key:
#        key: https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg
#        state: present
#
#    - name: Install VSCodium
#      ansible.builtin.apt:
#        name: brave-browser
#        state: presentcom
#    - name: Add an Apt signing key, uses whichever key is at the URL
#      ansible.builtin.apt_key:
#        url: https://ftp-master.debian.org/keys/archive-key-6.0.asc
#        state: present
#
#    - name: Add Docker repo
#      ansible.builtin.apt_repository:
#        name: docker
#        description: Docker repo
#        baseurl: "https://download.docker.com/linux/fedora/{{ ansible_distribution_major_version }}/{{ ansible_architecture }}/stable"
#        gpgkey: "https://download.docker.com/linux/fedora/gpg"
#        gpgcheck: true
#        enabled: true
#
#    - name: Install Docker
#      ansible.builtin.apt:
#        name:
#          - docker-ce
#          - docker-ce-cli
#          - containerd.io
#          - docker-buildx-plugin
#          - docker-compose-plugin
#        state: present
#
#    - name: Add Docker group
#      ansible.builtin.group:
#        name: docker
#        state: present
#
#    - name: Add user to docker group
#      ansible.builtin.user:
#        name: "{{ remote_regular_user }}"
#        groups: docker
#        append: true

    - name: Check if nvim is installed
      ansible.builtin.command:
        cmd: nvim --version
      register: nvim_installed
      ignore_errors: true
      changed_when: false

    - name: Download nvim
      ansible.builtin.get_url:
        url: "https://github.com/neovim/neovim/releases/latest/download/nvim-linux-x86_64.tar.gz"
        dest: "/tmp/nvim-linux-x86_64.tar.gz"
        mode: "0644"
      when: nvim_installed is failed or nvim_installed.stdout is not search(nvim_version)

    - name: Extract nvim to /opt/nvim-linux-x86_64
      ansible.builtin.unarchive:
        src: "/tmp/nvim-linux-x86_64.tar.gz"
        dest: "/opt/"
      when: nvim_installed is failed or nvim_installed.stdout is not search(nvim_version)
    
    - name: Add nvim to alternatives
      community.general.alternatives:
        name: "editor"
        link: "/usr/bin/editor"
        path: "/opt/nvim-linux-x86_64/bin/nvim"
        priority: 20

    - name: Ensure fonts directory
      ansible.builtin.file:
        path: "~{{ remote_regular_user }}/.local/share/fonts"
        state: directory
        mode: "0755"
        owner: "{{ remote_regular_user }}"

    - name: Check if Inter Font exists
      ansible.builtin.shell: "ls ~{{ remote_regular_user }}/.local/share/fonts/Inter*"
      register: inter_font_exists
      ignore_errors: true
      changed_when: false

    - name: Download Inter Font
      when: inter_font_exists is failed
      ansible.builtin.unarchive:
        src: https://github.com/rsms/inter/releases/download/v4.1/Inter-4.1.zip
        dest: "~{{ remote_regular_user }}/.local/share/fonts/"
        remote_src: true
        mode: "0755"
        owner: "{{ remote_regular_user }}"

    - name: Create Jet Brains Mono folder if it not exists
      ansible.builtin.file:
        path: "/usr/share/fonts/truetype/JetBrainsMono"
        state: directory
        mode: '0755'

    - name: Download and extract Jetbrains mono
      ansible.builtin.unarchive:
        src: https://github.com/ryanoasis/nerd-fonts/releases/download/v3.4.0/JetBrainsMono.zip
        dest: "/usr/share/fonts/truetype/JetBrainsMono/"
        creates: "/usr/share/fonts/truetype/JetBrainsMono/JetBrainsMonoNerdFont-Bold.ttf"
        remote_src: true
        mode: "0755"

    - name: Install debian.griffo.io repo key
      get_url:
        url:  https://debian.griffo.io/EA0F721D231FDD3A0A17B9AC7808B4DD62C41256.asc
        dest: /tmp/debian.griffo.io.asc 
    - name: dearmor the downloaded signing key file
      shell: gpg --batch --dearmor -o /etc/apt/trusted.gpg.d/debian.griffo.io.gpg /tmp/debian.griffo.io.asc
      args:
        creates: /etc/apt/trusted.gpg.d/debian.griffo.io.gpg 

    - name:  Add debian.griffo.io repo
      apt_repository:
        repo:  "deb [signed-by=/etc/apt/trusted.gpg.d/debian.griffo.io.gpg] https://debian.griffo.io/apt stable main"
        state: present
        update_cache: yes
    
    - name:  Install yazi
      package:
        name:  yazi
        state: present
